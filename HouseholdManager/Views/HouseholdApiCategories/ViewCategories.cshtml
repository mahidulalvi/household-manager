@model List<CategoryViewModel>
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ViewCategories</title>
</head>
<body>
    <div>
        @if (ViewBag.HasErrored != null)
        {
            <p style="color: red">Something's not right!</p>
        }
        else
        {
            for (var i = 0; i < Model.Count(); i++)
            {
                <p>@Html.ActionLink(Model[i].Name, "ViewCategory", "HouseholdApiCategories", routeValues: new { householdId = Model[i].HouseholdId, categoryId = Model[i].Id }, htmlAttributes: null)</p>
            }
        }
    </div>
</body>
</html>
